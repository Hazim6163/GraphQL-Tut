type Query {
  info: String!
  """list of links"""
  feed: [Link!]!
  """Get single Link by It's ID"""
  link(id: ID!): Link!
}

type Mutation{
    post(url: String!, des: String!) : Link!
    updateLink(id: ID!, des:String!, url: String!): Link
    deleteLink(id: ID!) : Link
}

type Link {
    """Auto gernated by the Database"""
    id: ID!
    """Link Description"""
    des: String!
    """Link URL"""
    url: String!
}